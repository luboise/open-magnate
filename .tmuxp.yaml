session_name: open-magnate

windows:
- window_name: Lazy Git
  panes:
    - shell_command:
      - lazygit

- window_name: backend
  layout: main-vertical
  options:
    main-pane-width: 70% 
  panes:
    - shell_command:
      - nvim ./backend

    - shell_command:
      - cd ./backend
      - npm run dev

    - shell_command:
      - mariadb -u prisma --password=password open_magnate_dev

- window_name: frontend
  layout: main-vertical
  options:
    main-pane-width: 70% 
  panes:
    - shell_command:
        - nvim ./frontend

    - shell_command:
        - cd ./frontend
        - npm run dev

