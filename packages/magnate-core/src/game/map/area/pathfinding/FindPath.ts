import { Position } from "..";
import { GameMap } from "../..";

export function FindPath(
	_map: GameMap,
	_pos1: Position,
	_pos2: Position
): /* PathDetails | */ null {
	// TODO: Reimplement
	return null;
	// const node = new DijkstraPriorityQueue(map, pos1, pos2);
	// return node.solve();
}
